# Flask  based browser where u enter url and u can browse it
# start the server using main.py.py  u get a default address   go to address and enter url
# after u  enter url it will show directory


# Install

```pip install flask ```

# Run

```python main.py.py```

